#include "replace.hpp"

void replace(std::string filename, std::string s1, std::string s2) {
    ofstream tempFile;
    tempFile.open (filename.txt);
    tempFile << "my dog is cuter than your dog, but your cat is cuter than my cat" << std::endl;

    for (std::string i; i != eof(); ++i)
}